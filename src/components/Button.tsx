import { Button } from "@nextui-org/button";

export default function CustomButton() {
  return (
    <Button
      style={{
        backgroundColor: "#32CD32", // Warna hijau kustom
        color: "white", // Warna teks
      }}
    >
      Yuk Login...
    </Button>
  );
}
